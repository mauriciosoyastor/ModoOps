# Grafo (snapshot congelado)

La UI vive en `/grafo` y el JSON en `web/public/grafo-data.json`.

**Sin motor de índice activo.** El snapshot está congelado hasta elegir e integrar un motor de grafos nuevo. No hay reindex ni CLI de consulta en este directorio.
