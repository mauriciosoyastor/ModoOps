{
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build -w galaxygroup-web",
    "deploy": "wrangler deploy --config wrangler.toml --no-autoconfig"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
