# PROTOTYPE — Capacidades × crafts por superficie (ticket 04 → 05)

## Landing (8 Astro + chrome)

Capacidades: Hero / Problema / Solución / Camino / Descubrimiento $155 / Para-quién / Cómo-trabajamos / Contacto + sticky CTA móvil + `Section/Button/SectionHeading` + `SiteHeader` + `FloatingWhatsApp`.
Crafts: tipo primero (tracking display + balance) / monocromo + acento star-warm quirúrgico / bandas full-bleed / 1 CTA verb-first por sección / quietud (única motion nueva: sticky bar) / foco visible global / ES-AR voseo, USD sin mezclar / perf LCP <2.5s.

## Control Plane consultor (`modoops_admin`)

Capacidades: tabla tenants full-width (nombre, DB, vertical, módulos, estado chip+texto, vencimiento, backup) + row actions (Ver, Suspender/Reactivar + overflow) + filtros facetados + veredicto mora arriba-izq + logs "showing X of Y".
Crafts: table-first / color = estado + texto / filtros reversibles con URL / foco visible + teclado (Enter abre detalle) / targets ≥44px / confirm en destructivas.

## Shell Tenant operativo (POS + stock/compras/fiscal)

Capacidades: POS Full 2 col (grilla + receipt, denominaciones ARS + pay-exact, stock inline, historial por caja, atajos) + cobro QR desktop (ticket 06) + tablas stock/compras/fiscal facetadas + bulk bar + error AFIP con causa + reintento.
Crafts: densidad operativa (8–12px zona primaria) / numéricos tabulares / superficies solo si agrupan / foco en Cobrar-Nueva-venta / state machine de pago (imposible éxito sin confirmación) / log por caja.
